[project]
name = "datamate-backend"
version = "0.1.0"
description = "DataMate backend service - AI data engineering platform"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"
